<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>My Bookings</title>
  <!-- <link rel="stylesheet" href="style.css"> -->
  <script src="script.js"></script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background-color: #f5f7fa;
      color: #333;
      padding: 40px 20px;
    }
    .title {
      font-size: 32px;
      font-weight: 400;
      text-align: center;
      margin-bottom: 40px;
      color: #2c3e50;
    }
    
    .no-bookings {
      text-align: center;
      font-size: 20px;
      color: #888;
      margin-top: 100px;
    }

    table {
      width: 90%;
      max-width: 1000px;
      margin:  40px auto;
      border-collapse: collapse;
      border-radius: 12px;
      overflow: hidden;
      background-color: white;
      box-shadow: 0 4px 8px rgba(0,0,0,0.05);
    }

    thead {
      background-color: #008080;
      color: white;
    }

    th, td {
      padding: 14px 20px;
      border-bottom: 1px solid #ddd;
      text-align: center;
      font-size: 16px;
    }

    tbody tr:hover {
      background-color: #f0f8ff;
    }

    td:last-child {
      font-weight: bold;
      color: #008080;
    }
    

    @media (max-width: 768px) {
  table {
    border: 0;
  }

  thead {
    display: none;
  }

  tr {
    display: block;
    margin-bottom: 15px;
    background: white;
    box-shadow: 0 2px 6px rgba(0,0,0,0.05);
    border-radius: 10px;
    padding: 13px;
  }

  td {
    display: flex;
    justify-content: space-between;
    padding: 10px;
    font-size: 14px;
    border-bottom: 1px solid #eee;
    position: relative;
  }

  td::before {
    content: attr(data-label);
    font-weight: bold;
    color: #444;
  }

  td:last-child {
    border-bottom: none;
  }
}

    
  </style>
</head>
<body>
   
  <h2 class="title">My Bookings</h2>

  <% if (bookings.length === 0) { %>
    <p class="no-bookings">No bookings found yet.</p>
  <% } else { %>
    <table>
      <thead>
        <tr>
          <th>Event</th>
          <th>Date</th>
          <th>Location</th>
          <th>Tickets</th>
          <th>Booked On</th>
          <th>Total</th>
        </tr>
      </thead>
      <tbody>
        <% bookings.forEach(b => { %>
          <tr>
            <td data-label="Event"><%= b.eventName %></td>
            <td data-label="Date"><%= b.date.toISOString().split('T')[0] %></td>
            <td data-label="Location"><%= b.location %></td>
            <td data-label="Tickets"><%= b.tickets_booked %></td>
            <td data-label="Booked On"><%= new Date(b.booking_date).toISOString().split('T')[0] %></td>
            <td data-label="Total">₹ <%= b.tickets_booked * b.price %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
</body>
</html>
